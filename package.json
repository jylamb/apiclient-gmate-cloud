{
  "name": "apiclient-gmate-cloud",
  "version": "1.0.0",
  "description": "Gmate Cloud API Test Client for Cloudflare Pages",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --binding CLIENT_ID=test CLIENT_SECRET=test API_BASE_URL=http://localhost:8080",
    "deploy": "wrangler pages deploy public",
    "tail": "wrangler pages deployment tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
